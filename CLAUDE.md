# CLAUDE.md — Scriber（暂定名）

## 项目范围（重要）

2026-09-16，用户明确推翻原 dayscribe 方向，重新缩小目标：

**做一个 macOS 上可以一键录音 / 录屏的工具软件。**

Scriber 是用户提出的候选名称，目前用作工作名。

当前实现与验收以 [SPEC.md](SPEC.md) 为准；[README.md](README.md) 是入口，[IDEAS.md](IDEAS.md) 保留背景和讨论记录。

## 工程重点

- 优先打通「开始录制 → 停止 → 保存可播放文件」的完整流程。
- 交互简单，录制状态明确，文件保存可靠。
- 录音已确认支持电脑声音和麦克风，两者分别开关。
- 录视频同时保存带声音的视频和独立音频；只录音保存一个合成音频文件。
- 录屏范围已确认支持整块屏幕、单个窗口、用户框选区域。
- 常驻菜单栏，点击图标或快捷键呼出面板；实时显示时长、可编辑文件名和两个声音来源的活动；在面板中设置保存路径。
- 用户当前强调好用、美观、人性化的交互差异；先迭代 UI，参考 Owl 的菜单栏浮层形态。
- 第一版只供 Yao 自用。录音可能超过 6 小时，视频通常不超过 2 小时；有线和蓝牙耳机都需覆盖。
- SPEC.md 区分已确认需求和实现默认值。默认采用本机 macOS 26+ / Apple Silicon、原生 Swift、MP4 + M4A；以当前原型作为 UI 基线。
- 8 小时录音、2 小时录屏是长测基准，不是录制时长上限；必须如实记录真实采集与硬件测试结果。
- 避免主动扩大到转录、摘要、访谈、日记、云服务或其他产品。

## 历史材料

- `docs/archive/dayscribe/`：旧构想和竞品分析，仅供历史参考，不是当前设计约束。
- 旧 ASR 实验代码保留在 `feat/phase0-asr-probe` 分支与 PR #4，不是 Scriber 的应用骨架，不需要为新方向维护或扩展。
- 原方案的 ASR、对象存储、Claude API、VAD 等技术选择不再适用。

## 当前阶段

主面板使用 AudioRecorder，真实双路选择、电平、最近文件和异步退出已通过内置/USB实测。每个声音源独立 SCStream，避免 captureMicrophone=false 后仍采集麦克风。全屏录制已在 --display-video-check 测试入口打通：ScreenVideoOutput + VideoSampleWriter 生成有声音的 MP4，同一混音 PCM 写入独立 M4A，使用公共 host-clock 起点和48k终点；Retina尺寸取 SCK contentRect×pointPixelScale。声音切换、双路/单路/USB录屏及 AppKit 退出通过。115 个组件用例及原生构建通过。CaptureTarget 已支持屏幕、独立窗口和区域几何；主屏、左侧外接屏、竖屏上的真实窗口/区域四色测试通过。采集规范为 sRGB，编码显式标记颜色，避免显示器色彩空间导致偏色。正式录屏入口和 NativeCapturePicker / RegionOverlayView 已通过原生 AX、HID 鼠标及系统键盘操作验收；修复了未激活的选区窗口首次拖动被激活行为吞掉的问题。当前应用可带 --ui-validation-report 写入手工验收的本地状态，不应把合成视图事件或离屏截图当作实际点击验证。design/ 是已确认的视觉基线。RecordingSessionFiles 在目标卷独立临时目录写入，收尾后通过 RecordingFileSet 防覆盖保存并更新实际 URL；真实音频/视频的录制中改名接口与 AppKit 退出通过，面板已开放录制前/中/保存后改名并通过真实键盘、AX、HID 和双文件解码测试，保存位置子页已接入异步系统文件夹选择器，分别持久化两种目录；当前目录在开始时冻结，修改下次生效。真实 GUI 选择、取消、重启记忆、音视频新旧目录写入和选择器打开时退出均通过。标准 Edit 菜单使输入框快捷键工作；主浮层关闭时取消未确认的名称编辑。每次录制保留小型 session.json；它不是崩溃恢复实现。RecordingHistoryStore 在媒体开始前持久化会话引用，manifest 持有当前名称/路径/时长/采集错误，改名保留元数据。索引采用排他锁与原子写入；损坏索引不会被覆盖。实际音视频登记与跨进程读取通过；历史列表/搜索、已知目录旧会话发现、持久化最近录制和 Finder 定位已通过实际 GUI 验证；详情已接入真实 AVPlayer/AVKit，支持默认暂停、播放/暂停/进度定位和视频/音频切换，收起面板暂停、返回释放播放器；详情历史改名已接入，使用每会话锁、最新记录路径和稳定文件标识；失败的记录更新不会使旧界面快照覆盖新路径。当前录制的改名同步更新 recorder URL，退出会等待改名完成。已通过原生控件验证，并用真实系统声音采集确认播放器输出的已知测试音。历史刷新合并请求并在读取失败时保留旧快照，定位文件前重新检查当前记录。全局呼出快捷键默认⌥R，可在原生子页修改/关闭并持久化；使用主线程系统热键注册，冲突保留旧绑定，从后台呼出主录制面板且不打断录音。两路声音行已显示真实设备名称；CoreAudio 后台持续读取系统默认设备和可用状态，录制中名称保留实际绑定的麦克风 UID。单路错误与缺少数据会隔离失败流，保留其余来源；全部中断时安全收尾，错误随会话保留。真实流中断、缺少数据、原生提示与手动关闭/开启恢复已验证，已实现有限重试与默认输入变化后的重连，真实数据到达才报告恢复，新增来源失败保留原录制；双路/单路恢复、耗尽重试、录屏配对、重连中 AppKit 退出已验证。原生重连提示与关闭麦克风不自动重开已在解锁后实测；物理设备切换仍待实测。编码器已启用原生片段索引，M4A 使用仅音频的 MPEG-4 容器以与视频保持崩溃前的 AAC 起始裁剪一致；真实音频与静态窗口强制终止后可解码前缀已验证。系统确认静止时每秒保持上次真实画面，并监测帧状态中断；录制会话在发布描述文件前取得系统文件锁，持有到编码器与最终元数据完成；正常保存及进程死亡后的释放已验证。已实现保留原始字节的完整箱体前缀复制、原生重新封装与全轨解码验证，并通过真实中断媒体及截断副本验收；恢复结果已能在双锁与持久日志保护下写回同一条记录，并在移动或写入中断后重试；原始数据保留，已保存成员复用，视频音轨可补回损坏的独立音频。应用启动已自动检查和恢复未完成录制，保留原历史身份；新录制暂停恢复、结束后继续，退出等待取消完成。实际崩溃重启、再次重启不重复、恢复中退出续接和原生历史播放已验证。开始前及录制中已检查实际可用磁盘空间与卷身份，低空间自动收尾；编码器异步写入失败会被及时报告。独立测试盘低空间停止、拒绝再次开始、释放空间后继续和进程级真实写入失败已验证。录制和保存期间持有原生系统防闲置睡眠断言，录屏同时防闲置熄屏；主动睡眠通知等异步收尾后才确认。真实音视频断言持有与释放及通知顺序组件测试通过，实际睡眠/合盖/锁屏转换仍待验证。录屏目标消失时，编码器成功收尾的视频会正常发布并保留采集中断提示；真实编码失败仍保留为未完成。恢复前缀复制已按块释放 Foundation 临时对象，实际大文件复制内存已验证保持有界。完整证据与剩余项见 docs/VALIDATION.md。

## 约定

- 对话用中文
- 提交走 `分支 → PR → 合并`，commit 需 SSH 签名、author 为 `luyao618`（见 `/PR-personal`）
- 用户明确要求：Goal 过程中每完成一个可独立验证的小部分就立即提一个小 PR，禁止最终一次性提交大 PR，也不能以一个大 PR 中的多个 commit 替代。具体要求见 SPEC.md 第 9 节。
- 用户已明确授权：每个小 PR 通过检查和自审后自动合并，再继续下一个 PR，不必逐个请求批准。报告 PR 链接、范围、验证及合并结果；每次从最新默认分支建立下一条 codex/ 分支。
- `.omc/` 已 gitignore，不要提交
