# CLAUDE.md — dayscribe

## 项目范围（重要）

个人实验性项目，作者自用。**不发布、不做多用户、不商业化。**

因此以下话题**不在设计约束内，不要主动提出**：

- **隐私 / 录音合规** —— 录音只在公共场所进行，不涉及他人隐私，无法律问题。已论证过，不必重复。
- 多租户、账号体系、权限模型
- 生产级部署、SLA、水平扩展
- GDPR / 数据留存策略 / 用户数据导出等合规设施

本地处理仍然优先，但理由是**省钱和速度**，不是合规。

把工程精力放在真正的难点上：ASR 质量、长文本归纳、提问质量、产出的可读性。

## 项目是什么

用全天录音 + 大模型访谈生成每日手账。核心链路：

```
录音 → 转录 → 事件归纳 → 记事员提问 → 用户回答 → 生成手账
```

差异化不在录音或转录（那是通用能力），而在**那个会追问你的「纪录片记事员」** ——
录音只有客观发生的事，提问把主观的那一半找回来。任何取舍优先保护这一环。

完整构想见 [IDEAS.md](IDEAS.md)。

## 当前阶段

构想阶段，**尚无代码**。下一步是验证提问环节（详见 IDEAS.md 第 3、5 节）。

## 约定

- 对话用中文
- 提交走 `分支 → PR → 合并`，commit 需 SSH 签名、author 为 `luyao618`（见 `/PR-personal`）
- `.omc/` 已 gitignore，不要提交
