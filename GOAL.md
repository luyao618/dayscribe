# Scriber v0.1 — 可复制的 Goal Prompt

以下内容用于启动 Goal；本文件本身不会启动实现或创建 PR。

```text
/goal 按 /Users/yao/work/code/personal/dayscribe/SPEC.md 实现 Scriber v0.1，以 /Users/yao/work/code/personal/dayscribe/design/index.html 为 UI 基线，交付可在我当前 Apple Silicon / macOS 26 的 Mac 上双击运行的原生 Scriber.app。

产品目标：
- 常驻菜单栏，点击或快捷键呼出已确认风格的面板。
- 真正录制电脑声音与麦克风，分别开关、显示真实电平；录音输出一个音频文件。
- 支持区域、窗口、全屏录制；录屏同时输出带声音的视频与独立音频。
- 实现实时计时、录制中改名、保存路径设置、自动保存，以及历史播放、改名和 Finder 定位。
- 支持有线、蓝牙和内置音频设备，处理设备切换、长时间写入和异常恢复。
- 按 SPEC.md 完成 8 小时录音和 2 小时录屏的真实时间测试；它们是验收基准，不是软件的硬时长上限。

硬性要求：按小 PR 持续交付。
1. 开始前读取项目约定与设计说明，检查当前分支、已有 PR 和未提交改动，保留现有需求文档与 UI 原型。先制定按小 PR 划分的计划，每个 PR 写明单一目标、依赖和验收方式；将现有文档与原型整理为合适的基础 PR，避免混入后续功能 PR。
2. 每完成一个可独立审阅、可构建、可验证的小部分，就立即签名提交、推送并创建一个 PR，再推进下一部分。不要等整个应用完成才提 PR，也不要用一个大 PR 中的多个 commit 代替多个小 PR。
3. 每个 PR 只解决一个内聚问题，通常保持在数百行量级的可审阅改动。明显过大时优先拆分，但不要为了缩小 diff 制造无法运行的中间状态。
4. 提 PR 前自审 diff，运行与本次改动对应的构建和测试，修复实际问题。PR 描述写清改动、原因、验证结果和局限；涉及 UI 时附截图。长时测试在功能就绪后集中进行，不必每个小 PR 都重复长测。
5. 我已授权：每个 PR 在检查通过、自审完成后自动合并，不必逐次询问。遵守仓库必需检查和保护规则。确认合并完成并同步远程默认分支后，再从最新基线创建下一个 codex/ 分支，保持后续 PR 的 diff 干净。
6. 提交遵循项目个人 GitHub 约定：作者 luyao618，使用 SSH 签名，验证签名与身份。不直接在主分支提交。
7. 每个 PR 都报告链接、本次完成内容、验证结果和合并结果，并更新工作计划中的 PR 清单。不能临近结束时才一次性批量提交所有 PR。

推进方式：
- 普通工程细节自行决定，持续实现、检查、修复并推进，不在每一步停下来请求确认。
- 保留已认可的 UI 和范围，不扩展到 AI 总结、转录、日记、云服务或对外发布。
- 遇到必须由我操作的系统授权、物理设备切换或真实阻塞时，说明具体需要什么，同时继续不依赖它的工作。
- 不用模拟电平、浏览器原型、合成时间戳或加速测试冒充真实采集、硬件验证或长时测试；测试结果必须如实记录。

完成条件：
交付实际可运行的 Scriber.app、原生源代码、可重复构建脚本、简短使用说明和 SPEC.md 要求的测试记录；所有属于本目标的 PR 已通过检查并合并。最终汇总应用位置、构建和运行方法、PR 列表、验收结果及剩余限制。有必需验收未完成时，不要宣称目标全部完成。
```
