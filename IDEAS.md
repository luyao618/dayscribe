# Scriber — 产品需求与讨论记录

> 更新：2026-09-16。Scriber 为候选名称。

实现与验收以 [SPEC.md](SPEC.md) 为准；本文件保留产品背景与讨论结论。

## 已确定的方向

做一个 **macOS 一键录音 / 录屏工具**。原 dayscribe 的目标过大，旧方案整体废弃。

用户需要记录时能快速开始，结束后能拿到音频或视频文件。

### 已确认的使用需求

- 使用范围：Yao 自己使用；当前为 Apple Silicon Mac，macOS 26.6.2。
- 单次录音可能超过 6 小时，录屏一般不超过 2 小时。
- 有线耳机和蓝牙耳机都可能使用，不能只验证一种声音设备。
- 首要场景：临时留下一段声音，包括会议、通话、视频里的内容，随手开始录。
- 录音来源：电脑播放的声音和麦克风都支持，可以分别开关。
- 录屏范围：第一版支持整块屏幕、单个窗口、用户框选的区域。
- 开始录制：每次先打开小面板，确认录音/录屏、声音来源和录屏范围，再点开始。
- 录后管理：文件自动保存，应用内提供简单的录制历史列表，支持播放、改名和在 Finder 中定位文件。
- 应用入口：常驻 macOS 菜单栏，点击图标或使用快捷键呼出小面板。
- 录制中直接显示时长、文件名，并允许就地修改文件名。
- 电脑声音和麦克风各自显示实时声音活动，用户能确认是否有声音输入。
- 录屏通过区域选择进入录制；此前确定的窗口与全屏范围仍保留。
- 录音和录屏的保存位置可以直接在面板中设置和查看。

### 交互设计方向

用户选择以“好用、美观、人性化的菜单栏面板”作为差异化重点。Owl 截图用作常驻菜单栏与浮层形态参考。

第一版交互原型已在本地完成，将由独立 PR 纳入仓库；设计说明见 [design/DESIGN.md](design/DESIGN.md)。
覆盖待机、录音中、录屏中、范围选择、保存位置、历史和录制详情。原型采用演示数据，尚未实现真实录制。
用户已肯定这版视觉方向，接下来以它作为原生应用实现的 UI 基线，补齐交付范围与真实采集验收标准。

### 当前工作方式与痛点

用户目前主要观看由他人录制的 Teams 会议。获取原始录音或录屏文件不方便，申请下载权限也麻烦，影响后续交给 agent 总结、分析和沉淀。

Scriber 要解决的是：用户自己录制会议，结束后直接拿到本地音频或视频文件，作为后续 agent 工作的输入。这里的“原始文件”指录制生成的可直接使用的媒体文件，并非要求无压缩音视频。

当前讨论的主流程：

```text
会议开始 → 启动 Scriber 录制 → 会议结束并停止 → 获得本地媒体文件 → 交给 agent 总结、分析、沉淀
```

### 已确认的录制模式与文件产物

| 模式 | 文件产物 |
|---|---|
| 录视频 | 一个带声音的视频文件，同时保存一个独立音频文件，共两个文件 |
| 只录音 | 按开关采集电脑声音和麦克风声音，最终合成一个音频文件 |

录视频时独立音频可以直接交给 agent 处理，视频本身也必须包含声音。
只录音时不为电脑声音和麦克风分别输出文件。
实现默认采用 M4A 音频、MP4 视频；具体编码与首次默认状态见 [SPEC.md](SPEC.md)。

## 第一版实现默认值

以下是根据当前讨论采用的实现默认值，不是用户逐项指定的参数；具体技术细节可以在保留已确认行为的前提下调整。

1. 面板记住上次选择；最终应用提供可配置快捷键，原型 Option+R 不视为必须沿用。
2. 主操作在开始和录制中保持相同位置；停止后自动保存。
3. 历史列表按一次录制组织；录视频生成的音频和视频归为一条记录，改名时保持关联。

“一键”的目标是入口随手可用。按已确认的交互，每次都通过小面板确认设置后开始，不能跳过面板直接按上次设置录制。
快捷键冲突和系统授权通过原生实现处理。

## 先不纳入

ASR、AI 摘要、访谈、手账、长期记忆、自动全天录制、视频剪辑、云同步和移动端。
旧方案中的云端 ASR、对象存储与 Web 后端均不作为新项目的前提。

## 实施与验收

- UI 方向已得到肯定，以当前原型为原生实现的视觉与交互基线。
- 面向本机自用，默认以 macOS 26+ / Apple Silicon 和原生 Swift 实现。
- 将超过 6 小时录音需求具体化为 8 小时长测目标；录屏采用 2 小时长测目标，软件不设对应硬上限。
- 优先关注设备切换、真实声音状态、持续写盘、异常恢复和音画同步。
- 系统工具的能力对比见 [COMPETITORS.md](COMPETITORS.md)。
- 完整验收、交付物和建议 Goal 文本见 [SPEC.md](SPEC.md)。

产品范围已足够开始实现；不需要为了开工继续穷举每项工程参数。

## 第一版验收建议

分别完成一次录音和录屏：开始方便、状态清楚、停止后文件可播放，包含用户所选的声音和画面。
录屏生成的视频有声音，并有独立音频；只录音生成一个合成音频文件。
权限被拒绝或保存失败时，能看懂发生了什么并采取下一步操作。

## 历史

旧构想与旧竞品调研见 [docs/archive/dayscribe/](docs/archive/dayscribe/IDEAS.md)。
它们记录过去的探索，不定义当前产品。
