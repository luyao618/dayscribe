# dayscribe — 想法笔记

> 用全天录音 + 大模型访谈，生成一份属于你自己的每日手账。
>
> 状态：构想阶段 · 最后更新 2026-09-01

**项目范围**：个人实验性项目，自用，不发布、不做多用户、不商业化。
录音只在公共场所进行，不涉及他人隐私，**隐私合规不是本项目的设计约束，讨论时不必展开**。

**关于本地处理**：早期草稿写的是"本地优先"，理由是省钱和速度。这条已经放宽——
ASR 改为**云端优先**（见第 3 节），因为归纳/提问/成文三步本来就在调 Claude API，
"本地项目"这个前提并不成立。真正的判据只有一条：**买质量比造质量划算**，
尤其当造出来的东西不是本项目的差异化。

---

## 1. 一句话

传统日记靠**回忆**来写，所以只剩下情绪和结论，细节全丢了。
这个项目让**录音负责细节、大模型负责追问、你负责补上只有你知道的部分**，最后合成一天的记录。

## 2. 核心构想

一天结束时，程序做四件事：

1. **听** —— 解析当天的录音，转成带时间戳的文字，尽量分清谁在说话。
2. **理** —— 结合你的人设与长期记忆（你是谁、身边有哪些人、在做什么项目），把流水一样的转录整理成"当天发生了什么"。
3. **问** —— 大模型扮演一个**纪录片记事员**：它读完素材，发现哪里有信息缺口，主动向你提几个问题。你回答，也可以补充照片。
4. **写** —— 综合转录、你的回答、照片，生成当天的手账。

### 为什么"提问"是关键的一步

如果只做「录音 → 转录 → 摘要」，产出就是一份冷冰冰的会议纪要。
真正让它变成"日记"的是第 3 步：**录音只有客观发生的事，没有你当时的想法。**
记事员的提问把主观的那一半找回来——它知道你今天下午和谁聊了 40 分钟，但不知道你聊完是什么感觉；它听到你说"那我先这样吧"，但不知道你在犹豫什么。

这也是它区别于普通"AI 日记 App"的地方：它不是让你从零写，而是**已经带着一天的素材来采访你**。

### 产出的调性

偏流水账是可以接受的、甚至是想要的——流水账保留的信息量最大。
但要在流水里**把精彩的部分挑出来加重**：意外的对话、情绪波动大的时刻、第一次做的事、和某个人关系的变化。

---

## 3. 开发路线：由简入繁

### 阶段一：MVP —— 验证核心可行性

**目标**：证明"录音 → 提问 → 手账"这条链路真的能产出**比自己写更好**的日记。
这是唯一要验证的事。不好用就说明构想有问题，早点知道。

**形态**：本地跑的后台程序 + 一个简单 Web UI。

**流程**：
1. 用户用自己的方式录音（手机自带录音 App 即可），手动上传单个或多个音频文件
2. **VAD 预处理**：切掉静音、按静音点分段，产出「时间映射表」（见难点 #1）
3. 逐段语音转文字，按映射表还原绝对时间，合并成一份带时间戳的当日转录
4. 大模型分析转录，输出结构化的"今日事件列表"
5. 大模型提 3–5 个问题，Web UI 上对话式回答
6. 生成当天手账（Markdown），可导出

**明确不做**：录音功能、拍照打点、GPS、移动 App、实时处理、多用户、账号体系。

> 「不做录音功能」这条值得重新审视：如果手机自带 App 的录音质量或分段行为不合用，
> 可能得自己录（见阶段二的实现约束）。但 MVP 阶段先不碰——**要验证的是提问环节，不是录音**。

**技术选型草稿**（待验证，不是定论）：
- 后端：Python + FastAPI —— 音频预处理生态在 Python 这边
- **ASR：云端优先，首选阿里云百炼 `qwen3-asr-flash-filetrans`**。理由见下方「为什么 ASR 走云端」
  - 兜底：如果单价不可接受，退回本地 `whisper.cpp`（MIT，53k★）。~~`faster-whisper`~~ 已停滞 9 个月，不用
- VAD 切段：`silero-vad`（MIT，10k★）——**这一步无论 ASR 在哪都要自己做**
- 说话人分离：**待定，取决于云端 ASR 是否自带**。不带则用 `pyannote.audio` 4.x，MVP 能分"我 / 其他人"就够。注意：这是所有竞品最一致的差评来源
- 大模型：Claude API（分析、提问、成文）
- 前端：先用最省事的方案（Vite + React 单页，或直接服务端渲染模板），UI 不是这一阶段的重点
- 存储：SQLite + 本地文件夹，一天一个目录；**外加一个对象存储 bucket**（云端 ASR 的硬性前提，见下）

#### 为什么 ASR 走云端

「本地 vs 云」听起来是架构选择，实际只影响**转录这一步**——录音是手动上传，
归纳/提问/成文本来就在调 Claude API。所以问题不是"要不要做本地项目"，
而是"ASR 这一步要不要外包"。

外包省掉的是：whisper.cpp 编译、模型下载、参数调优、词级时间戳对齐（whisperX 那层）、
中英混说的效果调试。这些都不是本项目的差异化。

**硬性前提：必须有公网可达的对象存储。**
DashScope 的接口收的是 URL 不是文件，它自己去拉：

```
presignGet(ossKey) → submit(audioUrl) → poll(taskId) → fetchResult(url)
```

所以"纯本地跑"和"用云端 ASR"不兼容，音频至少得先传到一个 bucket。
但**只需要一个 bucket**——不需要照搬 lyre 的整套（Workers + D1 + SSO + Web 仪表盘）。
那些是他要做成自托管 SaaS 才有的。一个本地 Python 程序 + 一个 bucket + SQLite 就够。

#### 开工前必须验的两件事

一次实验同时验完：拿一段真实录音（多人对话、中英混说）跑一遍云端 ASR。

| # | 要验什么 | 为什么是阻塞项 |
|---|---|---|
| 1 | **每小时单价** | VAD 后每天约 2h → 一年 730h。个位数元/小时就用；三位数就退回本地。**这是唯一能推翻云端方案的数字** |
| 2 | **是否自带说话人分离** | lyre 靠"系统音一轨 / 麦克风一轨"天然分离，**这招我们用不了**——公共场所所有人共用一个麦克风。API 不支持就得跑 pyannote，架构变混合 |
| 3 | 中英混说的实际准确率 | 难点 #2。顺带对照一下本地 whisper.cpp |
| 4 | `emotion` 字段填什么 | 见下方「一个可能的免费信号」 |

> **一个可能的免费信号**：DashScope 的句子级响应里带 `emotion` 字段。
> 如果真的可用，直接接上难点 #4 的"情绪词密集处"和阶段二的心情色带，
> 不用等 LLM 从文本里猜。但 lyre 的 mock 里全是 `"neutral"`，真实行为未经确认。

**参考实现**：[`nocoo/lyre`](https://github.com/nocoo/lyre)（MIT）的 `packages/api/src/services/asr.ts`
把 submit / poll / fetchResult 三段写得很干净，含多 channel 合并与词级数据分层存储
（句子级入库、词级归档到对象存储、按需拉取）。可近乎照抄。
详见 [COMPETITORS.md](COMPETITORS.md)。

### 阶段二：最终版 —— 独立 App

**形态**：手机 App，端上完成录制与处理。

- 自带**录音**（后台长时录制）、**拍照打点**、**GPS 轨迹**
- 生成图文并茂的精美手账
- 可视化：GPS 轨迹地图、Timeline、Dashboard
- **心情用色彩呈现**——一天的情绪曲线映射成色带，一年下来是一张情绪的年历
- 当天心情与大事件分析

#### 长时录音的实现约束

从 [lyre](https://github.com/nocoo/lyre) 的 macOS 录音层读出来的教训，
这三条会影响 App 形态的可行性：

**① "常开"必须实现成分段滚动写入，不能是一个大文件。**
lyre 录一次 = 一个 `AVAssetWriter` 从 `startWriting()` 写到 `finishWriting()`，
而 m4a 的索引（moov atom）是在收尾那一刻才落盘的。
**录到第 15 小时崩溃，前 15 小时全废**——文件在磁盘上但没索引，播放器和 ASR 都读不了。

正确做法：每 N 分钟、或每次静音超过阈值就切一个文件当场收尾。
崩溃最多丢一段，而且**天然顺带解决了难点 #1 的切片问题**。

**② 交互上"点一次一直开着"没问题，工程上的坑不在按钮。**
真正缺的是：睡眠/唤醒后自动续录（lyre 没有 `willSleep`/`didWake` 处理，
合盖就断且不会自己续上）、磁盘空间检查（lyre 也没有）。

**③ 我们不需要系统音，只要麦克风。**
lyre 那 1000 行音频管道文档、双 track PTS 对齐、sidecar 映射，
**全是为了合并"系统音 + 麦克风"两路才存在的**。
砍掉系统音那一路，直接用 `AVAudioRecorder` / 移动端等价物即可，
比 ScreenCaptureKit 轻得多，也不用申请屏幕录制权限。
**lyre 最复杂的那部分对本项目不相关。**

> 参考体量：128kbps 单声道 AAC ≈ 55 MB/小时，16 小时约 880 MB。
> 另外云端 ASR 通常有单文件时长/体积上限——lyre 的代码注释里写的是 12 小时 / 2GB
> （**注意这是二手信息，来自他人代码注释，未经阿里云官方文档核实**）。
> 但无论上限是多少，16 小时**都得切**，理由见难点 #1。

---

## 4. 已经能预见的难点

按"会不会卡死 MVP"排序。

| # | 难点 | 说明 | 初步想法 |
|---|------|------|----------|
| 1 | **信噪比** | 全天 16 小时录音里，可能只有 1–2 小时有实质内容，其余是静音、噪音、背景电视 | 先用 VAD 切出人声段，只对有语音的片段做 ASR。省钱也省时间——**工作量直接降到 1/8**。两个约束见下表 |
| 2 | **转录质量** | 中英混说、专有名词、人名、远场收音、多人重叠 | 把"人设与记忆"（同事名、项目名、常去的地方）作为 prompt/热词喂给纠错环节，让大模型基于上下文修正 ASR 的错别字 |
| 3 | **长文本进不了上下文** | 一天转录可能好几万字，塞不进一次调用 | 分段（按时间/场景）→ 每段摘要 → 层级归纳成事件列表。map-reduce |
| 4 | **提什么问题** | 提问质量直接决定成品质量。问得太泛（"今天过得怎么样"）等于没问 | 让模型显式找"信息缺口"：时长久但内容模糊的片段、情绪词密集处、只有半截的对话、突然出现的新人名 |
| 5 | 存储 | 16h 音频，Opus 低码率压缩后约 100–150 MB/天，一年 ~50 GB | 处理完只留转录，音频可选保留 N 天 |
| 6 | 手机端功耗 | iOS/Android 后台长时录音的系统限制与耗电 | 阶段二再说，但会影响 App 形态的可行性 |
| 7 | 成本 | 每天的 ASR + 多次大模型调用 | VAD 先砍掉 7/8 的量；大模型成本靠分段摘要控制。**做一次单日成本实测**（见第 3 节待验事项 #1） |

### VAD 预处理的两个约束

这一步看着简单，但有两个坑会直接毁掉后面的提问环节：

**① 不要真的把静音删掉，要留时间映射表。**
如果把有声片段首尾相接拼成新文件，时间轴就毁了。而记事员提问**恰恰依赖真实时间**——
"你下午三点跟人聊了 40 分钟"这种问题，没有绝对时间就问不出来。产出应该是：

```
[(09:12:30, 09:14:05), (09:31:00, 09:52:18), ...]   ← 保留原始时间戳
```

每段单独转录，转出来的相对时间戳再加回段起点。**这个映射表比裁剪本身重要。**

**② 切分点必须落在静音处。**
固定按 N 分钟硬切会把句子劈成两半，ASR 在边界上必然出错。用 VAD 的静音区间当切分点，天然避开。

---

## 5. 待想清楚的问题

- **"人设与记忆"具体是什么？** 一份手写的 profile？还是随着日记积累自动生长的长期记忆库（人物表、地点表、项目表）？后者显然更有意思，但 MVP 可以先手写。
- **手账的模板长什么样？** 固定结构（时间线 + 精彩片段 + 心情），还是每天让模型自由发挥？可能需要先手写 2–3 篇"理想输出"当作 few-shot 样例和验收标准。
- **一天的边界在哪？** 按自然日切，还是按"睡觉"切？熬夜的日子怎么算。
- **失败了怎么办？** 如果录音质量差到转录不可用，链路应该优雅降级成"纯访谈式日记"，而不是直接报错。
- **验收标准是什么？** MVP 做完，怎么判断"成功"？建议：自己连续用 7 天，然后回头读——**能不能想起当时不记得的细节**。这是唯一真实的指标。
  补充一条对提问环节的判据：**一个问题如果能原样贴到任何一天的日记后面，它就是废问题。**好问题必须引用当天转录里的具体证据。

---

## 6. 灵感来源 / 相关产品

详见 [COMPETITORS.md](COMPETITORS.md)。三条最重要的：

- **Rewind.ai / Limitless Pendant** —— **失败案例**。2025-12 被 Meta 收购、停售硬件、Rewind 下线。同期 Humane 停产、Bee 被 Amazon 收编。全天录音硬件这个品类在 2025–26 集体退场，活下来的（Plaud、Pebble）都明确拒绝常开
- **Day One Daily Chat**（2026-03 上线）—— **最接近的竞品，核心构想已被实现**：聊天 → 追问 → 生成条目。但它从空白开始问，因为它对你的一天一无所知
- **Hobonichi 手账 / bullet journal** —— 想要的**成品调性**在这边。更重要的是它二十年的留存教训：所有成功案例都在做同两件事 —— **把承诺的单位缩到最小**、**明确许可跳过**

另加一条**可复用的上游**（不是竞品）：

- **[`nocoo/lyre`](https://github.com/nocoo/lyre)**（MIT）—— 自托管的会议录音管理平台：
  macOS 菜单栏 App 手动录制，上传到自己的阿里云 OSS，DashScope 离线转写，Web 端管理 + 词级回放 + AI 摘要。
  它把「**听**」这一环做完了，「理 / 问 / 写」三环基本空白（摘要 prompt 只有一行）。
  **可抄**：ASR 调用与轮询骨架、词级数据分层存储。
  **不可抄**：常开录音（它是手动点）、长音频切片（它整个文件直接丢给云端）、
  靠双 track 做说话人分离（我们所有人共用一个麦克风，这招不成立）。

> 顺带一个观察：lyre 在音频管道上写了 1000 行设计文档、跑 155 个 Swift 测试，
> 到了 AI 环节只有一行 prompt。这正好反向印证本项目的判断——
> 录音转录是通用能力，差异化在那个会追问你的记事员。**工程精力的分配应该是镜像的。**

**本项目的差异化**：不做"可检索的记忆库"，做**有叙事的一天**。
前者是数据库，后者是作品。核心区别在于那个会追问你的记事员 —— 而且它**带着当天的证据**来问。
