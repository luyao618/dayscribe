# Scriber v0.1 — 实现说明与验收标准

更新：2026-09-16。已有认可的 UI 原型，原生应用正按小 PR 分步实现；本说明描述完整交付要求。

## 1. 交付目标

为 Yao 自己的 Mac 制作一个可双击运行的原生 Scriber.app，常驻菜单栏，用于录下会议、通话与屏幕内容，得到可以交给 agent 处理的本地媒体文件。

UI 以用户已肯定的 design/index.html 为视觉与交互基线。design/DESIGN.md 描述界面结构，design/QA.md 只证明原型交互，不能作为真实录制能力的验收证据。

## 2. 用户已确认

- 只供自己使用；当前机器是 Apple Silicon，macOS 26.6.2。
- 录音可能连续超过 6 小时；录屏通常不超过 2 小时。
- 有线耳机、蓝牙耳机都可能使用，需要覆盖不同声音设备环境。
- 菜单栏图标或快捷键呼出面板；在面板中确认模式和声音来源后开始。
- 电脑声音、麦克风独立开关，分别显示真实的声音活动。
- 录音最终得到一个混合了所选声音来源的音频文件。
- 录屏最终得到一个带声音的视频文件，以及一个独立音频文件。
- 录屏支持自选区域、单个窗口和整块屏幕；默认交互是先选择范围，再开始录制。
- 录制过程中实时显示时长与文件名，文件名可以直接修改。
- 在面板里设置和查看录音、录屏的保存位置；录完自动保存。
- 简单历史列表，支持播放、改名和在 Finder 中定位文件。
- 以好用、美观、人性化的面板作为差异化重点。

## 3. 为实现采用的默认方案

本节是根据当前需求收敛出的实现默认值，不是用户逐项指定的参数。实现中可以调整具体 API、模块划分与编码参数，以满足已确认的行为；调整应说明理由。

| 项目 | 默认方案 |
|---|---|
| 平台 | macOS 26+、Apple Silicon，先验证当前机器 |
| 技术 | Swift，SwiftUI / AppKit 原生 UI，优先使用 ScreenCaptureKit、AVFoundation 等系统框架 |
| 录音 | M4A / AAC，所选声音混合到同一份文件 |
| 录屏 | MP4 / H.264，默认 30 fps，保证会议共享屏幕文字可辨；另存内容与时间对应的 M4A |
| 首次状态 | 默认录音，两路声音开启；之后记住用户选择 |
| 声音范围 | “电脑声音”指系统播放声音；选择某个录屏窗口只限定画面，不隐含只录该应用声音 |
| 设备 | 默认使用系统输入 / 输出设备；面板能识别实际输入设备和采集状态 |
| 快捷键 | 提供可配置的呼出快捷键；原型 Option+R 不视为必须沿用的组合 |
| 保存目录 | 默认 ~/Movies/Scriber/录音 与 ~/Movies/Scriber/录屏，可通过系统文件夹选择器修改 |
| 文件配对 | 录屏产生的视频与音频放在同一目录，共用基础名称；历史中归为同一次录制 |
| 文件名 | 默认使用日期时间，允许开始前、录制中和保存后改名；重名时避免覆盖已有文件 |
| 控制 | 主面板和菜单栏显示状态；第一版沿用开始 / 停止流程 |

不要求虚拟声卡、云端服务或额外常驻录音服务。所需系统录屏 / 麦克风权限应通过原生授权流程获得。

## 4. 关键交互与状态

### 录制流程

- 录音：打开面板 → 确认声音来源 → 开始 → 停止并保存一个音频文件。
- 录屏：打开面板 → 确认声音来源 → 选择区域 / 窗口 / 屏幕 → 开始 → 停止并保存视频与音频。
- 收起面板、切换应用不停止录制；菜单栏持续显示录制状态。
- 录制中锁定模式和录屏范围，文件名与声音开关仍可操作。
- 录制中更改保存位置从下一次生效；当前录制继续使用开始时的目录。
- 完成写入后才显示保存成功；录屏两份文件中若有一份失败，明确显示部分成功和可用文件。

### 声音状态

电平必须来自真实采集数据。区分正在收音、自然静音、用户关闭、设备失效 / 权限异常；不能用固定动画表示收到了声音。

开始时至少启用一路声音。录制中最后一路声音不能被无提示地关闭。保持视频和独立音频使用相同的混音内容与时间轴。

### 设备变化

- 验证内置设备、有线 / USB 音频设备、蓝牙耳机这些常见路径。
- 输入 / 输出设备改变或耳机断开时，及时更新状态；采集需要恢复时尝试重新连接系统默认设备，并明确提示切换结果。
- 不把用户关闭的麦克风自动重新开启，不静默改变用户选择。
- 某一路失效时保留仍可工作的来源；所有有效来源失效且不能恢复时，明确提示并安全结束当前录制。
- 不改变 Teams 的设备设置，不把采集到的麦克风声音重新播放出来。

## 5. 长录音与文件可靠性

把“6 小时以上”具体化为 **8 小时连续录音验收目标**；视频采用 **2 小时录制验收目标**。这是实现和测试基准，应用不设对应的硬时长上限。

- 音视频持续写盘，内存占用应有界，不能随录制时长积累全部素材。
- 采用分段临时文件或其他可恢复的写入方式，避免长录音因最终文件尚未收尾而整场不可用。
- 正常停止后，向用户交付约定的一份音频，或一份视频加一份音频；内部临时分段不增加用户整理负担。
- 应用异常退出后，能够识别未完成录制并恢复已完整写入的数据。未完成片段的损失范围与恢复限制应在测试记录中写明，不承诺零丢失。
- 监测写入失败与磁盘不足，及时停止或安全收尾，保留已有可用数据。
- 录制期间避免因为系统闲置自动睡眠而无提示中断；用户主动睡眠 / 合盖时安全收尾并记录中断。
- 锁屏行为需在目标系统实测；若系统使采集中止，应如实结束或标记异常，不能仍显示正常收音。
- 长时间后的音画同步、混音稳定性和停止保存耗时要有测量记录。

## 6. 实现顺序

1. 建立可构建、可签名用于本机运行的原生应用骨架，先完成短时真实系统声音 + 麦克风采集验证。
2. 优先将已认可的 UI 落地为原生面板，接入已实现的真实录制状态；未完成的入口明确禁用。此项按用户对原生面板偏离原型的反馈提前，不再推迟到功能实现之后。
3. 接入混音和录屏，验证三种范围、双文件输出及时间对应关系；继续在已对齐的面板中接入电平、改名、路径、快捷键和历史。
4. 加入设备变化、长时写入和异常恢复处理，完成端到端与长时测试。
5. 交付 Scriber.app、构建脚本、使用说明和实际测试记录。

## 7. 验收清单

| 验收项 | 通过标准 |
|---|---|
| 本机运行 | 可重复构建出 .app，双击启动，菜单栏可呼出和收起面板 |
| 真实声音 | 使用可辨识测试信号验证系统声音、麦克风分别采集与混音；关闭某一路后输出符合选择 |
| 音视频 | 区域、窗口、全屏都能录；视频有声音，独立音频可播放，音频内容及时间对应 |
| 设备 | 实测当前可用的内置、有线 / USB、蓝牙路径，记录设备型号和结果；检查设备断开 / 切换行为 |
| 操作 | 开始、停止、实时电平、录制中改名、目录设置、快捷键、历史播放与定位均调用真实实现 |
| 长录音 | 完成一次 8 小时真实时间录音测试，记录资源变化、实际文件时长、可播放性和结束保存表现 |
| 长视频 | 完成一次 2 小时真实时间录屏测试，检查起始和末尾音画同步与两个文件的对应关系 |
| 异常 | 缺少权限、设备失效、写入失败和中途退出不会显示虚假的成功或正常收音；可恢复已完整写入的数据 |
| UI | 根据原型人工看图和实际点击验证，不把模拟电平 / 浏览器原型验收当作原生应用完成 |

短样例、加速测试、合成时间戳测试可以尽早发现问题，但不能冒充真实时间长测。需要用户配合的物理耳机切换或系统授权应提出具体的一次性操作，同时继续不依赖它的工作。测试记录必须区分已完成、未通过与未能实测，不用模拟输入冒充硬件验证。

## 8. 交付物与边界

- 可在当前 Mac 使用的 Scriber.app。
- 原生源代码、可重复构建 / 本地运行脚本。
- 简短的首次启动、系统权限、声音选择和文件位置说明。
- 真实测试记录，包括长测时长、设备覆盖、异常恢复结果和剩余限制。

第一版聚焦自用录制工具，不恢复旧 dayscribe 的 ASR、摘要、访谈、日记链路。App Store 发布、其他人的安装兼容、云同步、视频编辑和按应用过滤声音不属于本次交付。

## 9. 小 PR 交付方式（用户明确要求）

Goal 过程中，每完成一个可独立审阅和验证的小部分，就立即提交并创建一个 PR。不能等全部功能完成后才提大 PR，也不能用同一个大 PR 中的多个 commit 代替逐步交付。

- 先制定按小 PR 划分的实施清单，给每个 PR 定义单一目标、依赖与通过标准。
- 先妥善保留并整理当前未提交的需求文档、归档与 UI 原型；必要时拆成文档基线和原型基线 PR，避免混入后续原生功能 PR。
- 每个 PR 只实现一个内聚部分，保持可构建、可验证；不要为了缩小 diff 制造无法运行的中间状态。
- 在相应部分完成并通过验证后立即创建 PR，再推进下一部分；不要先堆积多个部分，最后批量拆 PR。
- 优先保持数百行量级的可审阅改动；若明显过大，先进一步拆分。确实不可分的改动在 PR 中解释原因。
- 分支使用 codex/ 前缀；提交作者为 luyao618，按现有约定使用 SSH 签名并验证签名；不直接在主分支提交。
- 提 PR 前自审 diff，运行与该 PR 对应的检查。长时测试在功能就绪后集中进行，不要求每个小 PR 都重复 8 小时长测。
- PR 描述写清改动、原因、验证结果和局限；涉及 UI 时附相应截图或验证记录。
- 每个 PR 创建后向用户报告链接、本次完成内容、检查结果和下一步，并在工作计划中维护 PR 清单。
- 用户已明确选择：每个 PR 通过检查和自审后自动合并，再继续下一个 PR；无需逐个请求用户批准提 PR 或合并。
- 等待必需检查通过，处理本次改动的真实问题，确认合并完成后同步远程默认分支，再从最新基线创建下一个 codex/ 分支。不要绕过必需检查或仓库保护规则。
- 每个 PR 的 diff 只展示该小部分，不把已合并的前序改动带进后续 PR。若遇到暂时不能合并的外部阻塞，报告具体原因，继续可独立的验证或分析，不把后续功能堆回旧 PR。

建议按原生应用骨架、麦克风录音、电脑声音与混音、录制面板、录屏输出、范围选择、文件管理、历史播放、设备变化、长时可靠性和最终打包验证等部分拆分。具体边界可按实现中的耦合关系调整，小步提 PR 的节奏不可省略。

## 10. Goal 入口

可复制的完整目标提示见 [GOAL.md](GOAL.md)。
