# Scriber v0.1 验收概览

更新于 2026-09-18。**目标尚未全部完成。** 本页按 SPEC 汇总证据，详细过程和限制见 [VALIDATION.md](VALIDATION.md)，操作见 [USAGE.md](USAGE.md)。

## 当前候选应用

- 路径：`build/candidate/Scriber.app`，原生 Release 构建，已使用现有 Apple Development 身份签名并验证。
- 构建来源：`79295eb8ec0336128d69dbe835b83339cb6e9441`，Sources 树 `08c6e0f352e7808ed40235e22bb639494d456e32`。后续仅文档变更不影响该代码版本。
- 本次打包环境：Apple Silicon / arm64、macOS 26.7、Xcode 26.6（17F113）。应用最低要求仍为 macOS 26。
- 系统 LaunchServices 启动和权限检查通过；正常应用在隔离目录录下双路声音，AppKit 退出保存 **4.576833333 秒 / 219688 帧**。文件完整解码通过，再次启动恢复一条可用历史，个人历史未变。
- 本地证据：`artifacts/delivery-candidate-r2.json`、`delivery-candidate-r2-build.log`、`delivery-candidate-r2-smoke.log`、`delivery-candidate-jl471nk2/verified.json`。

八小时录音仍使用冻结的 `build/Scriber.app`。首次两小时录屏在约91分钟时因测试画面程序的字体渲染崩溃而中断，文件已恢复，但不算长测通过。第二次实际录屏已完成 **7200.35425 秒**，保留文件的独立复核通过：两次原生完整解码、FFmpeg 完整解码、配对音频一致，首尾音画偏差的中位数变化 **6.666 毫秒**。首次自动原生解码曾报错，后续未复现，原因未确定；原始失败记录保留，不能表述为首次自动验收无异常。详细证据见 VALIDATION.md 的两小时复核记录。

长测使用单独冻结的测试包；当前候选包已包含视频中断发布修正，以及长测开始后合并的恢复复制内存和模式／范围记忆修正，最终还需更新默认应用包。候选包已有短时正常生命周期验证，不把它等同于同一二进制的两小时长测。

## 按需求检查

| SPEC 要求 | 当前证据 | 尚需完成 |
|---|---|---|
| 本机原生菜单栏应用，可构建、启动、呼出和收起 | 系统启动、原生菜单栏操作已验证；候选包启动与正常录制再验证通过 | 解锁后的候选包最终操作检查 |
| 电脑声音、麦克风分别采集与混音，一个 M4A | 已用实际输入和可辨识测试音检查各路、混音、静音段及完整解码 | 八小时完整结果 |
| 开关来源、真实电平、最后一路保护 | 已验证真实 PCM、电平状态和原生开关；非工作来源不会显示正常收音 | 真实耳机转换覆盖 |
| 区域、窗口、全屏录屏，MP4 有声音并另存 M4A | 三种范围均有真实采集和原生选择器记录；两小时窗口实录双文件解码及首尾同步复核通过 | 纳入候选包最终界面检查 |
| 时长、录制中改名、保存后配对改名 | 原生键盘、冲突编号、文件哈希和元数据验证通过；候选包中断录音的警告、编辑／确认和改名后字节一致已实测 | PR #41 录屏警告／部分保存的实际点击 |
| 设置和记住两种目录；本次／下次目录明确 | 原生文件夹选择、取消、录制中更改及重启后写入验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 历史搜索、播放、改名、Finder 定位 | 原生操作、实际播放输出、重启恢复及缺失文件状态已验证；两小时媒体原生播放模型检查通过；候选包中断音频改名后重启显示历史警告已实测 | 录屏／部分保存的原生跟进；两小时播放器补测为静音后台检查 |
| 模式和范围类型记忆 | 实际候选包按钮、两次进程重启及范围独立保存通过；保持待机、不自动开始，原设置已恢复 | 无独立功能缺口，纳入最终交付检查 |
| 全局快捷键与配置 | 后台唤起、修改、关闭、冲突保留和重启验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 内置、有线／USB、蓝牙和断开／切换 | 内置及既有 Jabra USB 短采集已验证；有限重连和失败隔离有真实流测试 | 物理插拔、默认设备切换、蓝牙路径 |
| 有界内存、八小时录音 | 真实八小时测试进行中；持续记录内存、CPU、帧数和时钟偏差 | 完整时长、全文件解码、首尾信号、资源趋势、保存耗时 |
| 两小时视频、首尾音画同步 | 第二次实际录制7200.35425秒；完整解码、211875个画面、两份音频345617004帧一致；最大绝对偏差18.333毫秒，首尾变化6.666毫秒；峰值RSS96.7MiB，收尾0.10秒 | 首次原生验证报错的未复现观察保留；最终交付时确认测试包与候选包差异 |
| 崩溃后恢复已写入内容 | 真实采集强制终止、原生恢复、重启不重复、原始字节保留和事务重试已验证 | 长测后的交付审查，不承诺零丢失 |
| 磁盘不足、写入失败、部分成功 | 独立测试卷低空间停止／拒绝／恢复；真实进程写入失败和可用前缀恢复已验证 | PR #41 部分保存提示的实际界面跟进 |
| 防闲置睡眠、主动睡眠／合盖安全收尾 | 原生防睡眠断言持有／释放已验证；通知顺序有组件测试 | 真实睡眠、合盖及唤醒过程；系统 30 秒期限仍是限制 |
| 锁屏状态真实，不虚报收音 | 已观察锁屏期间两项长测的实际帧数继续增长 | 受控锁屏／解锁转换及最终媒体检查 |
| 与已认可原型一致、实际点击验证 | 原型基线保留；既有菜单、范围、文件、历史、快捷键有原生截图／操作记录；候选包模式／范围记忆及音频保存警告已实际点击、看图 | PR #41 录屏／部分保存仍只有离屏图像及后端证据，需补实际点击 |
| 使用说明、源代码、构建脚本和测试记录 | 已提供，并核对当前控制名称、路径、权限和构建命令 | 全部必需验收结束后更新最终交付结论 |

## 证据边界和交付检查

短测、生成的编码器样本、注入的通知和离屏渲染各有明确用途，不能作为真实长时、物理设备或桌面点击通过的证明。长期测试只有完成录制、解码和结果审查后才能标记通过。

当前仓库审计确认：Goal 的 PR #5–#47 共 43 个均已合并，合并提交均在当前默认分支历史中。每个增量按个人身份、SSH 签名、自审和对应检查交付；原型自 PR #6 后未改动。旧 ASR PR #4／分支、原 stash 和 `.planning/scriber-v01/pre-goal-worktree` 备份仍保留。

最终交付前需确认：八小时录音结果通过、物理设备和睡眠／锁屏转换有直接证据、PR #41 录屏及部分保存原生跟进完成、最新应用包与已合并代码一致，并更新本页及验证记录。PR #47 与 PR #41 音频警告的原生跟进已完成。两小时录屏已有保留文件复核通过的证据，但首次原生验证异常仍须随交付记录。当前不会以候选包可启动或组件测试通过替代剩余条件。
