# Scriber v0.1 验收概览

更新于 2026-09-18。**目标尚未全部完成。** 本页按 SPEC 汇总证据，详细过程和限制见 [VALIDATION.md](VALIDATION.md)，操作见 [USAGE.md](USAGE.md)。

## 当前应用包

- 路径：`build/Scriber.app`；保留同版本副本 `build/candidate/Scriber.app`。原生 Release 构建，已使用现有 Apple Development 身份签名并验证。
- 构建来源：`2a86beb94fff756650252cb645f169495f06d47d`，Sources 树 `b061be2af52f3cfe2cb33c41d4239170844c915d`，包含录屏目标不可用的中文提示。后续仅文档变更不影响该代码版本。
- 本次打包环境：Apple Silicon / arm64、macOS 26.7、Xcode 26.6（17F113）。应用最低要求仍为 macOS 26。
- 系统 LaunchServices 启动和权限检查通过；更新后的正常应用在隔离目录录下双路声音，AppKit 退出保存 **4.553104167 秒 / 218549 帧**。文件完整解码通过，再次启动恢复一条可用历史，个人历史未变。
- 本地证据：`artifacts/delivery-candidate-r3.json`、`video-target-message-validation-build.json`、`delivery-candidate-r3-smoke.log`、`delivery-candidate-lov6h_kk/verified.json`。候选二进制 SHA256：`240e6ac6cc2026eaf4d5d2932151aab257767575725c749c3540f2e00f79991a`。
- 八小时采集退出后，默认包已更新为同一签名二进制。默认路径再次通过正常启动、双路录制、AppKit 退出、完整解码和重启历史检查：**4.385833333 秒 /210520 帧**。旧长测包已原样归档。证据：`artifacts/delivery-default.json`、`delivery-default-smoke.log`、`delivery-candidate-nl3mp871/verified.json`。

八小时录音已完成，原长测包录下 **28800.16 秒**并保存。全文件原生／FFmpeg 解码、首尾测试音、原生跳转播放与资源复核通过；应用保留了麦克风设备变化后恢复的警告，原严格无警告检查失败。**这不是麦克风全程无缺口的证明。**

首次两小时录屏在约91分钟时因测试画面程序崩溃而中断并恢复，不算两小时通过。第二次完成 **7200.35425 秒**，保留文件的完整解码、配对音频及首尾同步复核通过，音画偏差中位数变化 **6.666 毫秒**。首次原生验证报错未复现，原因未定；原失败记录保留。两项长测的范围和限制详见 VALIDATION.md。

长测使用冻结的测试包；当前默认包和候选副本包含之后的修正，均有短时正常生命周期验证。它们不等同于同一二进制重新完成了长测；各版本来源均保留。

## 按需求检查

| SPEC 要求 | 当前证据 | 尚需完成 |
|---|---|---|
| 本机原生菜单栏应用，可构建、启动、呼出和收起 | 系统启动、原生菜单栏操作已验证；候选包启动与正常录制再验证通过 | 解锁后的候选包最终操作检查 |
| 电脑声音、麦克风分别采集与混音，一个 M4A | 实际输入和可辨识信号、混音、静音段通过；八小时单文件完整解码及首尾信号通过 | 设备切换期间的麦克风连续性不由该长文件结果证明，纳入设备验收 |
| 开关来源、真实电平、最后一路保护 | 已验证真实 PCM、电平状态和原生开关；非工作来源不会显示正常收音 | 真实耳机转换覆盖 |
| 区域、窗口、全屏录屏，MP4 有声音并另存 M4A | 三种范围均有真实采集和原生选择器记录；两小时窗口实录双文件解码及首尾同步复核通过 | 纳入候选包最终界面检查 |
| 时长、录制中改名、保存后配对改名 | 原生键盘、冲突编号、文件哈希和元数据验证通过；音频、录屏双文件及部分保存的实际警告／编辑／确认通过，已保存成员改名后字节一致 | 纳入最终包一致性检查 |
| 设置和记住两种目录；本次／下次目录明确 | 原生文件夹选择、取消、录制中更改及重启后写入验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 历史搜索、播放、改名、Finder 定位 | 原生操作、实际播放输出、重启恢复及缺失文件状态已验证；中断录屏重启显示警告并可播放视频；部分保存默认播放可用音频，未完成视频预览禁用 | 纳入最终包一致性检查；两小时播放器补测为静音后台检查 |
| 模式和范围类型记忆 | 实际候选包按钮、两次进程重启及范围独立保存通过；保持待机、不自动开始，原设置已恢复 | 无独立功能缺口，纳入最终交付检查 |
| 全局快捷键与配置 | 后台唤起、修改、关闭、冲突保留和重启验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 内置、有线／USB、蓝牙和断开／切换 | 内置及既有 Jabra USB 短采集已验证；有限重连和失败隔离有真实流测试 | 物理插拔、默认设备切换、蓝牙路径 |
| 有界内存、八小时录音 | 实际28800.16秒、1382407680帧；全文件双解码、首尾信号、原生播放通过；峰值RSS85.094MiB，收尾0.077秒 | 原无警告检查失败已保留；36次重连及混合原生采样率不能证明麦克风全程连续 |
| 两小时视频、首尾音画同步 | 第二次实际录制7200.35425秒；完整解码、211875个画面、两份音频345617004帧一致；最大绝对偏差18.333毫秒，首尾变化6.666毫秒；峰值RSS96.7MiB，收尾0.10秒 | 首次原生验证报错的未复现观察保留；最终交付时确认测试包与候选包差异 |
| 崩溃后恢复已写入内容 | 真实采集强制终止、原生恢复、重启不重复、原始字节保留和事务重试已验证 | 长测后的交付审查，不承诺零丢失 |
| 磁盘不足、写入失败、部分成功 | 独立测试卷低空间停止／拒绝／恢复；真实写入失败和恢复已验证；实际部分保存标签、改名、历史和可用音频播放通过，原始未完成视频保留 | 纳入最终包一致性检查 |
| 防闲置睡眠、主动睡眠／合盖安全收尾 | 原生防睡眠断言持有／释放已验证；通知顺序有组件测试 | 真实睡眠、合盖及唤醒过程；系统 30 秒期限仍是限制 |
| 锁屏状态真实，不虚报收音 | 已观察锁屏期间两项长测的实际帧数继续增长 | 受控锁屏／解锁转换及最终媒体检查 |
| 与已认可原型一致、实际点击验证 | 原型基线保留；菜单、范围、文件、历史、快捷键以及模式记忆、三类保存警告均有实际操作／截图；录屏目标不可用已有中文提示和实测 | 交付包最终检查；物理设备及电源交互仍单列待验收 |
| 使用说明、源代码、构建脚本和测试记录 | 已提供，并核对当前控制名称、路径、权限和构建命令 | 全部必需验收结束后更新最终交付结论 |

## 证据边界和交付检查

短测、生成的编码器样本、注入的通知和离屏渲染各有明确用途，不能作为真实长时、物理设备或桌面点击通过的证明。长期测试只有完成录制、解码和结果审查后才能标记通过。

当前仓库审计确认：Goal 的 PR #5–#54 共 50 个均已合并，合并提交均在当前默认分支历史中。每个增量按个人身份、SSH 签名、自审和对应检查交付；原型自 PR #6 后未改动。旧 ASR PR #4／分支、原 stash 和 `.planning/scriber-v01/pre-goal-worktree` 备份仍保留。

最终验收仍需确认：设备切换与麦克风重连情况有充分证据，实际睡眠／锁屏转换完成。默认包已更新，各类原生保存警告跟进已完成。两项长文件已有上述复核结果，原失败／警告及麦克风连续性限制必须随交付记录；当前不会据此宣布剩余设备、电源验收完成。
