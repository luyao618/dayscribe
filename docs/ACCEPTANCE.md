# Scriber v0.1 验收概览

更新于 2026-09-18。**目标尚未全部完成。** 本页按 SPEC 汇总证据，详细过程和限制见 [VALIDATION.md](VALIDATION.md)，操作见 [USAGE.md](USAGE.md)。

## 当前候选应用

- 路径：`build/candidate/Scriber.app`，原生 Release 构建，已使用现有 Apple Development 身份签名并验证。
- 构建来源：`406a60ddf3fd8f209e5cd6b148d839e7b3bb87e1`，Sources 树 `800fab40e7a31e54693614209ffab19521c22279`。后续仅文档变更不影响该代码版本。
- 本次打包环境：Apple Silicon / arm64、macOS 26.7、Xcode 26.6（17F113）。应用最低要求仍为 macOS 26。
- 系统 LaunchServices 启动和权限检查通过；正常应用在隔离目录录下双路声音，AppKit 退出保存 **4.555229167 秒 / 218651 帧**。文件完整解码通过，再次启动恢复一条可用历史，个人历史未变。
- 本地证据：`artifacts/delivery-candidate.json`、`delivery-candidate-build.log`、`delivery-candidate-smoke.log`、`delivery-candidate-l1fx_09p/verified.json`。

两项长测仍分别使用冻结的 `build/Scriber.app` 与 `artifacts/video-geometry-app/Scriber.app`。它们的二进制未被候选包替换。长测结束后再更新默认应用包并做最终检查。

## 按需求检查

| SPEC 要求 | 当前证据 | 尚需完成 |
|---|---|---|
| 本机原生菜单栏应用，可构建、启动、呼出和收起 | 系统启动、原生菜单栏操作已验证；候选包启动与正常录制再验证通过 | 解锁后的候选包最终操作检查 |
| 电脑声音、麦克风分别采集与混音，一个 M4A | 已用实际输入和可辨识测试音检查各路、混音、静音段及完整解码 | 八小时完整结果 |
| 开关来源、真实电平、最后一路保护 | 已验证真实 PCM、电平状态和原生开关；非工作来源不会显示正常收音 | 真实耳机转换覆盖 |
| 区域、窗口、全屏录屏，MP4 有声音并另存 M4A | 三种范围均有真实采集和原生选择器记录；两份音频解码一致 | 两小时完整同步结果 |
| 时长、录制中改名、保存后配对改名 | 原生键盘、冲突编号、文件哈希和元数据验证通过 | PR #41 异常保存后编辑／确认的实际点击 |
| 设置和记住两种目录；本次／下次目录明确 | 原生文件夹选择、取消、录制中更改及重启后写入验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 历史搜索、播放、改名、Finder 定位 | 原生操作、实际播放输出、重启恢复及缺失文件状态已验证 | PR #41 警告行的原生界面跟进 |
| 全局快捷键与配置 | 后台唤起、修改、关闭、冲突保留和重启验证通过 | 无独立功能缺口，纳入最终界面检查 |
| 内置、有线／USB、蓝牙和断开／切换 | 内置及既有 Jabra USB 短采集已验证；有限重连和失败隔离有真实流测试 | 物理插拔、默认设备切换、蓝牙路径 |
| 有界内存、八小时录音 | 真实八小时测试进行中；持续记录内存、CPU、帧数和时钟偏差 | 完整时长、全文件解码、首尾信号、资源趋势、保存耗时 |
| 两小时视频、首尾音画同步 | 真实两小时测试进行中；短校准及故意错位副本的拒绝检查通过 | 完整时长、全文件解码、配对音频、首尾同步和资源趋势 |
| 崩溃后恢复已写入内容 | 真实采集强制终止、原生恢复、重启不重复、原始字节保留和事务重试已验证 | 长测后的交付审查，不承诺零丢失 |
| 磁盘不足、写入失败、部分成功 | 独立测试卷低空间停止／拒绝／恢复；真实进程写入失败和可用前缀恢复已验证 | PR #41 部分保存提示的实际界面跟进 |
| 防闲置睡眠、主动睡眠／合盖安全收尾 | 原生防睡眠断言持有／释放已验证；通知顺序有组件测试 | 真实睡眠、合盖及唤醒过程；系统 30 秒期限仍是限制 |
| 锁屏状态真实，不虚报收音 | 已观察锁屏期间两项长测的实际帧数继续增长 | 受控锁屏／解锁转换及最终媒体检查 |
| 与已认可原型一致、实际点击验证 | 原型基线保留；既有菜单、范围、文件、历史、快捷键有原生截图／操作记录 | PR #41 图像为原生离屏渲染，不能替代点击验收 |
| 使用说明、源代码、构建脚本和测试记录 | 已提供，并核对当前控制名称、路径、权限和构建命令 | 全部必需验收结束后更新最终交付结论 |

## 证据边界和交付检查

短测、生成的编码器样本、注入的通知和离屏渲染各有明确用途，不能作为真实长时、物理设备或桌面点击通过的证明。长期测试只有完成录制、解码和结果审查后才能标记通过。

当前仓库审计确认：Goal 的 PR #5–#42 共 38 个均已合并，合并提交均在当前默认分支历史中。每个增量按个人身份、SSH 签名、自审和对应检查交付；原型自 PR #6 后未改动。旧 ASR PR #4／分支、原 stash 和 `.planning/scriber-v01/pre-goal-worktree` 备份仍保留。

最终交付前需确认：两项长测结果通过、物理设备和睡眠／锁屏转换有直接证据、PR #41 原生跟进完成、最新应用包与已合并代码一致，并更新本页及验证记录。当前不会以候选包可启动或组件测试通过替代这些条件。
